// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://IggorOK.github.io',
  base: '/edodatok-pryroda-lyudyna/',
  output: 'static',
  trailingSlash: 'always',
  integrations: [],
});
